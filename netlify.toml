[build]
  publish = ".next"
  command = "pnpm run build"

[build.environment]
  NODE_VERSION = "22"

# Form submissions configuration
[[forms]]
  name = "job-application"
  [forms.settings]
    subject = "New Job Application - MCA Express"
